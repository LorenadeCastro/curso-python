# Aula sobre comparações de valores

# Utilizando os sinais: > (maior), < (menor), >= (maior ou igual), <= (menor ou igual), != (diferente de), == (igual)

# Se a tempuratura está acima de 30.
# O dia está quente.

temperatura = 30

if temperatura == 30:
    print("O dia está quente.")